import React, {useState} from 'react';
import axios from 'axios';

// function login

function Login() {
    // keep username setState for each user
    const [username, setUsername] = useState('');
    const [password, setPassword] = useState('');
}